# Database (MySQL)
# Example (XAMPP, empty root password): mysql://root@127.0.0.1:3307/road_guard
DATABASE_URL="mysql://root@127.0.0.1:3307/road_guard"

# Auth
JWT_SECRET="change_me_to_a_secure_random"
ADMIN_SIGNUP_SECRET="set_a_strong_admin_secret"

# CORS
# Comma-separated list if multiple origins are needed
CORS_ORIGIN="http://localhost:5173"

# Twilio (optional, reserved for future OTP/notifications)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
NOTIFY_VIA_TWILIO=false

# App
PORT=8000
